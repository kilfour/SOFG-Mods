using Xunit;
using Witching;
using Assets.Code;
namespace Witching.Tests
{
    public class Spike
    {
        [Fact]
        public void OhISeeItsUntestable()
        {
            // var map = new Map(new Params());
            // map.genEmptyMap();
            // var location = new Location(map, new Hex(), true);
            //var witch = new Witch(location, new Society(map, location), 1);

            // var result = witch.GetPower().Charges;
            // Assert.Equal(0, result);
        }
    }
}
